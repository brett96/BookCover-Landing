import ResetPasswordPage from "@/components/ResetPasswordPage";

export const metadata = {
  title: "Reset password — BookCover",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <ResetPasswordPage />;
}
